function cropped = cropImage(image, bBox)
%Crops img according to bBox
%INPUT  - image H*W*3
%       - bBox  1x4
%OUTPUT - cropped size of bBox

h_max = min(bBox(2)+bBox(4)-1, bBox(2));
h_min = max(bBox(1),1);

w_max = max(min(bBox(1)+bBox(3)-1, bBox(1)),1);

cropped = image(bBox(1):w_max, bBox(2):h_max,:);
%cropped=imcrop(image,bBox);
end

